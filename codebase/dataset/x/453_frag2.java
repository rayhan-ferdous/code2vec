            rv.append(";");

            rv.append(bouquet);

        }

        rv.append(CONF_SEPARATOR);

        rv.append(frequency);

        rv.append(CONF_SEPARATOR);

        rv.append(parameter);

        rv.append(CONF_SEPARATOR);

        rv.append(source);

        rv.append(CONF_SEPARATOR);

        rv.append(symbolRate);

        rv.append(CONF_SEPARATOR);

        rv.append(videoIds);

        rv.append(CONF_SEPARATOR);

        rv.append(audioIds);

        rv.append(CONF_SEPARATOR);

        rv.append(ttId);

        rv.append(CONF_SEPARATOR);

        rv.append(caId);

        rv.append(CONF_SEPARATOR);
